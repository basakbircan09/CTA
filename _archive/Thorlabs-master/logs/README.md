# Logs

Use this directory to store ad-hoc run logs, hardware notes, or test artifacts that should stay local to the repository. Add filenames to .gitignore if you want to keep them out of version control.
